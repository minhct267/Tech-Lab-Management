<Page x:Class="TechLabManagement.Views.BookingSchedulerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Scheduler">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="Booking Scheduler" FontSize="20" FontWeight="Bold" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,10">
            <TextBlock Text="Resource:" VerticalAlignment="Center" Margin="0,0,6,0" />
            <ComboBox Width="220" ItemsSource="{Binding Resources}"
                SelectedItem="{Binding SelectedResource}" DisplayMemberPath="Name" />
            <TextBlock Text="Date:" VerticalAlignment="Center" Margin="16,0,6,0" />
            <DatePicker SelectedDate="{Binding SelectedDate}" />
            <TextBlock Text="Start:" VerticalAlignment="Center" Margin="16,0,6,0" />
            <ComboBox Width="80" ItemsSource="{Binding TimeSlots}"
                SelectedItem="{Binding StartTime}" />
            <TextBlock Text="End:" VerticalAlignment="Center" Margin="16,0,6,0" />
            <ComboBox Width="80" ItemsSource="{Binding TimeSlots}" SelectedItem="{Binding EndTime}" />
            <Button Content="Book" Margin="16,0,0,0" Command="{Binding BookCommand}" />
        </StackPanel>

        <GroupBox Grid.Row="2" Header="Existing Bookings">
            <DataGrid ItemsSource="{Binding ExistingBookings}" AutoGenerateColumns="False"
                IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Start" Binding="{Binding Start}" Width="120" />
                    <DataGridTextColumn Header="End" Binding="{Binding End}" Width="120" />
                    <DataGridTextColumn Header="Resource" Binding="{Binding ResourceName}" Width="*" />
                    <DataGridTextColumn Header="User" Binding="{Binding UserName}" Width="120" />
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
    </Grid>
</Page>
