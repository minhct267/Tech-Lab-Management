<Page x:Class="TechLabManagement.Views.MaintenanceSafetyPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Maintenance &amp; Safety">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="Maintenance &amp; Safety" FontSize="20" FontWeight="Bold" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,10">
            <TextBlock Text="Equipment:" VerticalAlignment="Center" Margin="0,0,6,0" />
            <ComboBox Width="260" ItemsSource="{Binding Equipment}"
                SelectedItem="{Binding SelectedEquipment}" DisplayMemberPath="Name" />
            <TextBlock Text="Due &lt;= (days):" VerticalAlignment="Center" Margin="16,0,6,0" />
            <ComboBox Width="100" ItemsSource="{Binding DueDaysOptions}"
                SelectedItem="{Binding SelectedDueDays}" />
            <Button Content="Add Task" Margin="16,0,0,0" Command="{Binding AddTaskCommand}" />
            <Button Content="Export CSV" Margin="16,0,0,0" Command="{Binding ExportCsvCommand}" />
            <Button Content="Print" Command="{Binding PrintCommand}" />
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Tasks}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Due" Binding="{Binding DueDate}" Width="140" />
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="160" />
                <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="*" />
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="80" />
                <DataGridTemplateColumn Header="Actions" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Complete"
                                Command="{Binding DataContext.CompleteTaskCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
